# Exclude VS Code-specific and helper files
.vscode/
.vscode-test/
.vsc-extension-quickstart.md
.vscode-test.mjs

# Exclude test files from the `out` directory
out/test/
out/**/*.test.js  # Explicitly exclude test JS files in out

.gitignore
.yarnrc

# Exclude original TypeScript source files if the compiled JS is in `out/`
src/**/*.ts

# Exclude TypeScript configuration files and source maps
**/tsconfig.json
**/*.map
**/.eslintrc.json

# Exclude Aider-related files and directories
.aider*
.aider.tags.cache.v3/
**/.aider/**

# Exclude Claude-related project files
.claude/
CLAUDE.md

# Exclude any other unnecessary files
*.log
.DS_Store
Thumbs.db

# Exclude test files from root
test-*.js

# Exclude unnecessary files from node_modules dependencies
node_modules/**/*.md
node_modules/**/*.txt
node_modules/**/README*
node_modules/**/LICENSE*
node_modules/**/CHANGELOG*
node_modules/**/*.d.ts
node_modules/**/*.test.js
node_modules/**/*.spec.js

# TODO: Consider bundling with webpack/esbuild to reduce file count (1245 files)
# See: https://aka.ms/vscode-bundle-extension
# Note: Complex with native ExifTool binaries - future enhancement
